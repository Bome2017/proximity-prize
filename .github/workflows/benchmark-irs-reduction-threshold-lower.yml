name: IRS Reduction Threshold Lower Benchmark

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read
  actions: read

jobs:
  irs-reduction-threshold-lower:
    uses: ./.github/workflows/benchmark.yml
    with:
      profile: lower
